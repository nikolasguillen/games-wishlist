# GamesWishlist Project Rules

The project rules live in `CLAUDE.md` at the repository root, plus directory-scoped `CLAUDE.md` files under
`feature/` and `core/*`. Known deviations from those rules are tracked in `docs/tech-debt.md`.

Read the root `CLAUDE.md` before changing any code.
